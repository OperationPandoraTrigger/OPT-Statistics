import { makeStyles } from "@material-ui/core/styles";

export const useCampaignScoreStyles = makeStyles((theme) => ({
  root: {},
  matchSelector: {},
  button: {},
  nested: {
    paddingLeft: theme.spacing(4),
  },
}));
